name: Cron Test

on:
  schedule:
    - cron: "*/5 * * * *"
  workflow_dispatch:

jobs:
  cron-test:
    runs-on: ubuntu-latest

    steps:
      - name: Show current time and confirm cron fired
        run: |
          date
          echo "cron test success"
